import 'package:flutter/material.dart';

class CoursesPageDesktop extends StatelessWidget {
  const CoursesPageDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Course page desktop"),
    );
  }
}
